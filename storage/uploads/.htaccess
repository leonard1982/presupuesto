# Proyecto PRESUPUESTO - Si esta carpeta llega a ser publica, bloquear ejecucion de scripts.
<FilesMatch "\.(php|phtml|phar|php[0-9]*)$">
    Require all denied
    <IfModule !mod_authz_core.c>
        Deny from all
    </IfModule>
</FilesMatch>
